<!-- generated by weave from Linear issue TAG-138 — do not hand-edit -->

### TAG-138: Example 12.40 The invocation expression of the greatest common divisor

**Status:** ✅ Implemented

## Software Notes

Remaining [TAG-138](https://linear.app/taguniversal/issue/TAG-138/example-1240-the-invocation-expression-of-the-greatest-common-divisor) work items:

1. Generate valid component declarations or shell entities for unresolved invocations such as `LT`, `Mod`, `dualfanin`, `EQ0`, and `dualfanout`.
2. Generate structural port maps connecting invocation arguments, intermediate signals, and outputs.
3. Emit valid placeholders for unresolved invocation bodies instead of TODO-only comments.
4. Drive unresolved outputs with `null_value` rather than leaving them undriven.
5. Resolve repeated boundary names such as `condition` without duplicate VHDL ports.
6. Handle pure-value and conditional expressions such as `Equal($A,0)` and `$steer()` without undeclared signals.
7. Preserve local versus parent signal scope across nested definitions.
8. Add missing component/library dependency handling during GHDL verification.
9. Implement the actual GCD/NCL behavior, including feedback, conditional routing, and sequencing.

Already completed:

* Numeric literal emission.
* Empty-boundary completeness.
* Port-list punctuation.
* Grouped and mutex boundary parsing.
* Grouped-name flattening.
* Mutex completeness expressions.
* Intermediate signal declarations.
* Multi-source lookup emission.
* Nested definition emission.
* NCL package integration.
* Reserved-word sanitization.
* Clean scratch-directory handling in `verify.sh`.

## Reference

```
// linear: TAG-138 The invocation expression of the greatest common divisor

GCD(MN) ( GCD< > )

GCD[(M< > N< > ) ($GCD)

init< 0 >

LTsteer($M,$N)(inlesser< > ingreater< > )

dualfanin($incondition {$inlesser $remainder}{$ingreater $cyclelesser})
  (lesser< > greater< > )

Mod($cyclelesser $cyclegreater)(remainder< > )

EQ0({$lesser $init})(incondition< > outcondition< > )

dualfanout($outcondition $lesser $greater)
  ({(cyclelesser< > cyclegreater< > ) ( SINK< > GCD< >) })
:
dualfanin[(steer< > {A< > B< >}{C< > D< >})($out1 $out2)
   $steer( ) : True[out1< $A > out2<$B >]
               False[out1< $C > out2<$D >] ]

dualfanout[(steer< > A< > B< >)({$out1 $out2} {$out3 $out4})
   $steer( ) :  True[out1< $A > out2<$B >]
                False[out3< $B > out4<$A >] ]

EQ0[(A<>)($condition $condition)
  condition<Equal($A,0)>:]

LTsteer[(A< > B< >)
  LT($A $B) : True[out1< $A > out2<$B >]
              False[out1< $B > out2<$A >] ]
] 
```
